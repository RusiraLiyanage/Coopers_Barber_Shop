# Shared config version used by this frontend for environment-wide values.
commonConfVersion:
  # AppConfig hosted configuration version for staging.
  staging: 1
  # AppConfig hosted configuration version for production.
  production: 1
# Service config version used by this frontend for Vite build-time values.
serviceConfVersion:
  # AppConfig hosted configuration version for staging.
  staging: 4
  # AppConfig hosted configuration version for production.
  production: 1
